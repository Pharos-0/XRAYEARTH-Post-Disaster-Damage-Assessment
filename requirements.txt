# ═══════════════════════════════════════════════════════════
#  XRayEarth — requirements.txt
#  Python 3.11 | CUDA 12.8 | RTX 5060 (Blackwell sm_120)
#
#  IMPORTANT: Install PyTorch FIRST before this file:
#    pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
#
#  Then install the rest:
#    pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── Core PyTorch (install separately — see note above) ────
# torch (nightly cu128) — installed manually before this file
# torchvision (nightly cu128) — installed manually before this file

# ── Computer Vision ───────────────────────────────────────
opencv-python==4.9.0.80
Pillow==10.2.0
albumentations==1.4.0
pycocotools==2.0.7

# ── Annotation & Geometry ─────────────────────────────────
shapely==2.0.3
numpy==1.26.4

# ── Data Handling ─────────────────────────────────────────
pandas==2.2.1
tqdm==4.66.2

# ── Config Management ─────────────────────────────────────
PyYAML==6.0.1
omegaconf==2.3.0

# ── Experiment Tracking ───────────────────────────────────
wandb

# ── Metrics ───────────────────────────────────────────────
scikit-learn==1.3.2
torchmetrics

# ── ONNX Export ───────────────────────────────────────────
onnx==1.15.0
onnxruntime-gpu==1.17.1

# ── Utilities ─────────────────────────────────────────────
matplotlib==3.8.3
seaborn==0.13.2
rich==13.7.1
python-dotenv==1.0.1

# ── TensorRT (optional — after training completes) ────────
# pip install tensorrt pycuda
# NOTE: Install manually after full training if TRT export needed